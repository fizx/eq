# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: postgresql
  database: eq_dev
  user: <%=ENV["USER"] %>
  password: 
    
test:
  adapter: postgresql
  database: test
  user: <%=ENV["USER"] %>
  password:
    
production:
  adapter: sqlite3
  database: db/production.sqlite3
  pool: 5
  timeout: 5000
